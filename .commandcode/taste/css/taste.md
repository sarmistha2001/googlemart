- Prefers a single centralized external stylesheet (e.g. `assets/css/style.css`) holding all CSS, linked from every page, rather than inline `<style>` blocks or per-page styles. Confidence: 0.95
- For reusable mega menu components, expects dedicated shared CSS classes (e.g. `.mega-heading`, `.mega-subhead`, `.mega-list`, `.mega-badge` with `.badge-popular`/`.badge-reco`/`.badge-new` color variants) in the central stylesheet rather than per-panel inline styling — the premium mega menu redesign was delivered as reusable classes so the treatment can be applied to every other mega panel and page consistently. Confidence: 0.6
- Brand accent palette is explicit and must be preserved exactly by hex value: `#da4886` (pink/magenta) for primary accent buttons (e.g. carousel "View all" buttons — filled pink with white text) and `#fff232` (cool yellow) for secondary controls (e.g. carousel prev/next buttons — filled yellow with dark text); the user states exact hex codes rather than approximate hues and asks to keep them ("Keep #da4886", "keep #fff232"). Confidence: 0.9
- Wants the site's theme/brand colors derived from and matched to the logo (e.g. "add the logo and change the theme color according to logo") rather than keeping arbitrary or pre-existing color choices — brand-consistent color theming. Confidence: 0.7
- Prefers uniform, consistent vertical spacing between all page sections — when section top margins are inconsistent (e.g. mixed `mt-4`/`mt-5`), standardize every section to the same top spacing ("correct the spacing for all section keep space from top"). Confidence: 0.7
- Prefers generous, ample vertical spacing between sections — after uniform spacing was set, asked for even more ("still add more space in top"), leading to a custom `mt-6` utility (4.5rem/72px) beyond Bootstrap's built-in `mt-5` applied to every section; again asked for extra space above every carousel ("add space in above of this section"), implemented via `margin-top: 1rem` on the swiper containers. When the first increase isn't enough, follows up with terse nudges ("add more space") to iteratively escalate the same spot until it looks right — e.g. the New Arrivals pagination dots were bumped `mt-3` → `mt-4` → `mt-5` across consecutive requests, and the agent is expected to keep increasing in the same direction without re-asking. Confidence: 0.95
- Prefers photographic background images over flat brand-color backgrounds for hero/banner sections and inner-page breadcrumb heroes ("instead of background color add a background image"; "make the breadcrumb image insteady of color"), accepting external image sources (e.g. Unsplash) with a dark overlay gradient so text stays readable, and expecting the change applied site-wide via the shared hero class. Confidence: 0.85
- Prefers that newly added design components (e.g. team cards, stat bands) be styled in the established brand theme colors — for this site, purple/magenta gradients (e.g. `linear-gradient(135deg, #3b0764, #701a75, #a21caf)`) with yellow accent numbers (`#fff200`) — so new sections blend seamlessly with the rest of the site rather than introducing colors from a reference image. Confidence: 0.8
- Wants sections that look too plain/simple upgraded to a "premium" look — rich dark gradient backgrounds, a colored accent line/glow, uppercase letter-spaced headings, hover micro-interactions (link slide + underline, social buttons lifting with brand-color fill), and polished details like payment badges (e.g. "footer is looking simple make like premium"; "make this section premium" for the store locator; "add a premium faq section above this" for the FAQ). The accepted premium treatment for the store locator was a rich purple gradient card with a yellow accent glow, white heading with a yellow highlighted word, frosted-glass translucent info chips, a subtle overlay on imagery, and working CTAs (Google Maps directions + tel: call); the FAQ got a centered badge/title header, numbered pill items with "+" icons that rotate on open, and a soft lavender gradient panel with hover lift. Confidence: 0.97
- Wants breadcrumb heroes upgraded to a premium frosted-glass pill — semi-transparent white background with backdrop blur, a home icon, yellow chevron separators, yellow hover glow on the Home link, bold white current-page text, and a soft shadow — requested tersely as "make the breadcrumb premium" and applied to every inner page via a shared `.premium-crumb` class so all breadcrumbs match the treated hero image behind them; reconfirmed when the user asked for the product page to get the same breadcrumb "like about". Confidence: 0.92
- Has a demanding, escalating bar for "premium": after a reference-faithful contact-page redesign the user still said "still not look premium", and the accepted upgrade added strong color contrast (dark purple gradient info panel beside a white form card), radial yellow-glow accent circles, a magenta gradient form header with frosted badge, rounded translucent icon tiles with yellow icons, and an extra gradient "bulk quote" CTA band between sections — i.e. "premium" means layered depth (gradients, glows, contrast, extra conversion touchpoints), not just matching layout/colors, and flat-looking first attempts will be rejected. Note: this premium escalation applies to showcase/banner sections — on the contact page itself the user later reversed course, asking to remove all gradients and keep it formal and simple (see the contact-page learning below). Confidence: 0.7
- Prefers a large, prominent brand logo in the footer — explicitly asked to make the footer logo bigger because it was "looking very small"; the accepted fix sizes the logo image explicitly (e.g. `height:64px;width:auto` with a white chip background, rounded corners and shadow) instead of the small `brand-logo-sm` utility, applied to the shared footer on every page. Confidence: 0.7
- Prefers icon+text contact lists (like the footer "Get in Touch" column) styled with a dedicated alignment class rather than reusing link-list styles: a fixed-width icon gutter (`flex-shrink: 0`) so all icons line up vertically, `align-items: flex-start` so multi-line rows (address, stacked phone numbers) wrap cleanly under the text, and no hover padding-shift that makes rows jump (reported "in footer get in touch column not aligned properly"). Confidence: 0.7
- For mobile layouts, prefers hiding elements that don't fit cleanly (e.g. a secondary hero card like "Photo Gifts & Mugs") using Bootstrap responsive visibility utilities (`d-none d-md-block`) rather than letting them shrink, wrap, or crowd the small screen — requested tersely as "hide this for mobile", and the accepted fix keeps the element visible from the `md` breakpoint (768px) up while removing it from mobile only. Confidence: 0.6
- For image-only full-width banners, prefers a clean framed treatment: `w-100 object-cover` with a fixed wide aspect ratio (`aspect-ratio:16/5`), rounded corners (`rounded-24`), soft shadow (`shadow-soft`), no overlay text or CTAs, and a higher-resolution Unsplash source (`?w=1400&q=80`) — applied when the user asked to strip a split hero down to "keep only image in full row". Confidence: 0.6
- For promo image grids that stay 2-across on mobile, prefers the images to be square (1:1) on small screens and restored to the wide banner ratio (16:7) on tablet/desktop — requested tersely as "for mobile show this 2 column in square shape"; accepted fix is a dedicated `.promo-square` CSS class with `aspect-ratio: 1/1` plus a `min-width: 768px` media query switching to `16/7`, replacing the previous inline wide-ratio style — i.e. responsive aspect-ratio treatment belongs in the stylesheet as a class with a breakpoint, not as an inline style. This is a site-wide pattern for ALL promo/banner image sections, not a one-off: when a second promo section (Banner 2) was still wide on mobile, the user pointed at it and asked for the same square-mobile treatment ("show this 2 col square shape in mobile view"), and the accepted fix added the same `.promo-square` class (dropping the redundant `row-cols-md-2` and the inline `aspect-ratio:16/7`) — so every promo image grid on the site is expected to share the same responsive square-on-mobile treatment. Confidence: 0.85
- Prefers product cards to use square (1:1) rounded images (e.g. `rounded-4 object-cover` with `aspect-ratio:1/1`) inside a padded card (`p-2`, `card-body p-2`) rather than 4:3 `card-img-top` images with badges. The canonical "match the home page" card the user points to is minimal — lucide icon + name + short description only: when told to change category page products "like [the home page Gift card markup] ... but not swiper slide", ratings, prices, and OFF chips were all dropped so cards match the reference exactly, so do not add price/rating rows unless the reference card has them. Confidence: 0.8
- For interactive elements with selected/active states (e.g. product image thumbnails), prefers a dedicated CSS class (`.thumb-btn` with `.active`) carrying cursor pointer, hover feedback (opacity fade), and brand-colored border on the active item — rather than repeated inline `border-color:var(--brand)!important` per element — so clickable affordance and selection state are consistent and maintainable. Confidence: 0.5
- Wants the main navigation bar to stay visible while scrolling — explicitly requested "make the navitem bar sticky"; implemented as a dedicated `.sticky-nav` class (`position: sticky; top: 64px; z-index: 1020`) so the nav sticks just below the fixed-height header. Confidence: 0.85
- Specifies the exact direction when adjusting spacing and expects the margin applied on that side only — e.g. for the New Arrivals pagination dots the user said "add above space not below", and the accepted fix increased the top margin (`mt-3` → `mt-4`) while leaving the spacing below untouched; when the user says "add space above/below", do not add padding on both sides or the opposite side. Confidence: 0.55
- For the contact page (and functional/info surfaces generally), prefers a formal, simple, clean look over decorative gradient treatments — explicitly asked to "remove background gradient keep formal simple", and the accepted fix replaced the purple contact panel and magenta gradient form header with plain white `page-card`s using the site's standard border + soft shadow; gradient/premium styling is for showcase/banner sections, not contact or functional cards. Confidence: 0.7
- Expects adjacent/paired buttons to be aligned consistently — identical heights (e.g. 38px for the Call Now / WhatsApp pair, 44px for the Send Message / WhatsApp Us pair), matching rounded styles, and equal gaps — and explicitly requests "correct the button alignments" when they are off. Confidence: 0.7
- For social media icon buttons on light/white surfaces (e.g. contact card), prefers a rounded colored circle background with white icons and adequate spacing between the buttons — refined iteratively: first "keep social media links background color" while fixing alignment, then "social media keep space between rounded backgound white icon" (`gap-3` spacing, white icons, no border), then "background circle with the color pink type", and again "add background in social icons keep more space and color of icon white" which pushed spacing to `gap-4` — the accepted final look is a `.social-btn-light` circle with a pink/Instagram-style gradient (`linear-gradient(135deg, #e4007c, #ff6fb5, #ff8cc8)`) and white icons rather than a flat solid brand color, while the footer's dark `rgba(255,255,255,0.08)` `.social-btn` style stays unchanged for the dark footer. The user keeps asking for "more space" between the icons across repeated requests and expects the agent to keep increasing the gap in the same direction (gap-2 → gap-3 → gap-4) without re-asking. Alignment of the icon inside the circle matters to the user ("keep ... with a background color proper alignment") — the accepted fix centers the glyph perfectly with `display:inline-flex` + `line-height:1` so the icon sits dead-center in the colored circle. Confidence: 0.9
- Prefers hover micro-interactions applied consistently to every same-type item once one instance is approved — after praising the mega menu hover effect ("when hover all the effet is so good"), asked to "apply it all items when hover same type design", and the accepted fix extended the treatment to all mega list links (magenta slide-right + growing left indicator bar) and all top-level nav items (animated brand-color underline bar + tinted background on hover/active) via shared classes rather than leaving it on a single item; reaffirmed when the user asked for the same mega menu hover design on every other nav item ("when hover same day delivery visiting card apprel personalised gift and other also"), which was delivered by applying the same hover effects to all 8 remaining panels. Confidence: 0.75
- Prefers the mega menu panel width to fit its content rather than stretching full-screen — explicitly asked to "adjust mega menu width accoding items size"; the accepted fix centered the panels container (`position-absolute start-50 translate-middle-x` with `width:min(1180px,94vw)` instead of `start-0 end-0` full-bleed) and overrode the inner `container-1280` to `max-width:100%` so the column grid sits within the compact, content-sized width. Confidence: 0.65
- Prefers the mega menu constrained to a fixed ~80% width rather than full-width: explicitly requested "keep the menu width like 80%", and the accepted fix added a shared `.menu-80` class (`max-width:80%` centered, with responsive fallbacks to 92% under 1400px and 100% under 1200px) applied to the desktop nav strip, the hover panels, and the mobile pills. Confidence: 0.6
- Prefers the mega menu hover panel to open at 80vw (viewport-width, centered under the nav strip) even though the nav strip itself stays at the 80% container width — explicitly requested as "the mega menu size correct when hover menu the mega menu will like 80vw"; the accepted fix set the panels wrapper to `position-absolute start-50 translate-middle-x` with `width:80vw;left:50%` and added responsive fallbacks in the stylesheet (92vw under 1400px, 96vw under 1200px) so the wide panel never overflows smaller screens. Confidence: 0.7
- For the admin panel specifically, prefers a simple, modern, functional visual language distinct from the front-end's premium/gradient styling — the accepted redesign used a dark sidebar with a purple accent for the active nav item, white sticky topbar, rounded cards, uppercase letter-spaced table headers, thumbnail images with a zoom-in cursor, subtle-tint badges and soft-focus inputs; "looking simple make look good user friendly" for the admin means clean whitespace and tidy alignment, not gradients/glows (which stay reserved for front-end showcase sections). Confidence: 0.65
- For compact category icon strips/carousels, prefers slightly larger cards with tighter gaps rather than a sparse original layout — asked to "make the category card slight big and reduce the gap between", accepted as icons bumped 72px → 88px and Swiper `spaceBetween` reduced (8 → 6 base, down to 10 on desktop) plus a tighter icon-to-label gap (8px → 4px); category strips should read denser and bolder, in contrast to the generous vertical spacing preferred between page sections. Confidence: 0.5
hin the compact, content-sized width. Confidence: 0.65
- For the site header, prefers the contact-first reference layout: a pink (#e4007c) announcement bar with business info, a sticky white header with the business logo image (e.g. 52px) on the left, a centered search bar (~720px max, 42px tall), a green (#25d366) circular WhatsApp icon, and a pink (#e4007c) rounded-pill "Contact Now" call button on the right (hidden below the md breakpoint) — requested by pasting the exact demo.html header with "keep the sticky header like...". Confidence: 0.85
ead denser and bolder, in contrast to the generous vertical spacing preferred between page sections. Confidence: 0.5
hin the compact, content-sized width. Confidence: 0.65
