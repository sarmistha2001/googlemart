# Root taste preferences
- Prefers communicating a desired look/layout via a reference (a website URL or a screenshot/image), expecting it to be studied and replicated faithfully ("ditto") rather than described from scratch. Confidence: 0.9
- When given a reference image for a page/section, expects the layout and structure to be replicated faithfully but with the site's own established brand theme colors substituted for the reference's colors (e.g. "make the about page design like this but colors from our theme") — the reference defines structure, not the palette. Confidence: 0.9
- Prefers incremental, section-scoped changes that preserve existing designs ("keep rest designs") instead of full redesigns — when converting index.html to dynamic PHP, the user explicitly said "keep same type mega menu dont change", i.e. approved design blocks must remain untouched while data is wired in. Confidence: 0.97
- When a static page is converted to a dynamic DB-driven page, expects the rendered output to preserve the original markup structure exactly — balanced divs, identical section nesting and spacing — and treats any resulting layout shift as a defect to fix, reporting it tersely ("the page alignment not correct") and expecting the agent to find the root cause (e.g. unbalanced/unclosed divs and leftover hardcoded slides left by a find-and-replace conversion) and restore the page until the output is structurally identical to the original (div-balance 0, all sections/sliders present). Confidence: 0.7
- Prefers a static grid over a slider/carousel for category/product listing pages: when asked to change category.html products to match a home-page swiper card, the user explicitly appended "but not swiper slide" and the accepted fix kept the 3-column static grid — carousel/swiper treatment is for home-page showcase sections, not listing pages. Confidence: 0.65
- For promo/banner card grids, prefers the columns to stay side-by-side even on mobile rather than stacking to a single column — explicitly asked the secondary promo section to "show 2 cols in one row in mobile", and the accepted fix changed the row from `row-cols-1 row-cols-md-2` to a plain `row-cols-2` so both promo images sit in one row on all screen sizes; responsive grids of paired banners should keep a compact 2-across layout on small screens unless stacking is requested. Confidence: 0.6
- Expects edits to be reversible and may ask to undo a previous change; benefits from version control or checkpoints so changes can be rolled back cleanly. Confidence: 0.8
- Prefers mega menu navigation (hover-revealed panels organized by category) over simple dropdown menus for large category catalogs, with panels shown only on hover; the panel width should be constrained and centered rather than stretching full-screen edge-to-edge — refined via "adjust mega menu width accoding items size" to a content-sized/centered panel, and later explicitly re-specified as a wide centered panel at 80vw ("the mega menu will like 80vw"). Confidence: 0.85
- When asking to undo/revert changes (e.g. "undo all previous changes"), expects the agent to act immediately and revert fully rather than ask clarifying questions about scope; the user expressed frustration when asked how far back to revert instead of just undoing — reconfirmed in this session when the user said "undo changes", the agent asked a clarifying question, and the user pushed back with "still not revert undo". Confidence: 0.8
- Relies on session IDs for continuity and expects prior session context to be retrievable/compiled for handoff between sessions — asked to "copy context from this session id <uuid>" so work can resume in another session; expects the agent to locate the stored session data (e.g. `~/.commandcode/...` jsonl) and produce a summary of prior asks and where things left off; reaffirmed with a follow-up "copy all" when the first pull only returned a partial/handoff summary, i.e. the user wants the full context copied, not just a pointer to where it lives. When the user refines to "copy only context", the accepted delivery is the full raw conversation (all user messages + assistant replies, in order) exported to a file in the project root (e.g. `session-context.txt`) that the user can open/copy — export the actual context content for handoff, not a summarized digest or a pointer to where the session data lives. Once exported, the user treats that file as the durable handoff artifact and starts the next session by telling the agent to "remember context @session-context.txt" — expecting the full file to be read/loaded (not just the compacted summary) so all prior work and open items are restored before continuing. Confidence: 0.8
- Prefers a short, curated set of top-level navigation items that group many sub-categories under broader labels (e.g. "Apparel", "Personalised Gifts", "Signage") rather than listing every category in the nav. Confidence: 0.7
- For recurring control elements, expects shared CSS classes applied consistently across every instance (e.g. adding `carousel-nav` / `carousel-view` classes to all carousel prev/next and "View all" buttons) so styling is applied site-wide rather than per-button, and verifies the class coverage count matches the actual number of sections. Confidence: 0.85
- Prefers full-width, single-image promo/banner sections over multi-column grids for banner areas — and when asked to simplify a hero, expects it reduced to the image alone ("keep only image in full row"), stripping all headline/badge/CTA/rating copy and overlay cards from the banner (e.g. the Business Cards hero was cut from a text+image split with a "Try the studio" overlay to a bare 16:5 full-width image with rounded corners and a soft shadow). Confidence: 0.75
- Prefers product showcase sections (bestsellers, new arrivals, etc.) as single-row horizontal auto-scrolling carousels matching the category carousel's behavior, rather than multi-row static grids — requested for new arrivals with an explicit slides-per-view count (6 on desktop); reaffirmed when asking for Trending and 8 category-highlight rows as carousels "like New Arrivals", and again when converting the Best Sellers section to the same auto-sliding carousel pattern. Confidence: 0.97
- For product showcase carousels, prefers a single item per row on mobile rather than 2-across — explicitly requested for the Best Sellers section ("for mobile in one row one item not 2"), implemented as a base `slidesPerView: 1` in the Swiper config that scales up at breakpoints (2 at 576px, 3 at 768px, 4 at 992px+), i.e. mobile carousel density should default to one item at a time and only increase with screen width. Confidence: 0.6
- Extends the auto-sliding carousel treatment to cross-sell sections on product pages: the Related Products section was explicitly asked to be converted to a slider "make the related product in sliding" using the home page carousel pattern, so showcase/cross-sell sections on any page should get the same carousel treatment rather than remaining a static grid. Confidence: 0.6
- Prefers driving work with terse, minimal, typo-laden prompts (e.g. "continue", "kepp all next prev buttons side of view all button", "keep only image in one row 4 itwm ... height like 300*400", "make the breadcrumb image insteady of color") and expects the agent to interpret intent and autonomously carry the in-flight task through to completion without stopping to re-plan, re-explain, or ask for confirmation at each step (e.g. "add a premium faq section above this"; "still not look premium" after a redesign, expecting the agent to autonomously escalate the design further; e.g. "app the header in all page" for propagating the updated header site-wide). Confidence: 0.92
- Reports bugs/errors by pasting the browser devtools console/network error text verbatim (e.g. "Failed to load resource: the server responded with a status of 500 ()") or terse fragments ("HTTP ERROR 500", "when add category showi", "fix this error" with an IDE file/line context) rather than describing the problem — expects the agent to use the snippet to locate the failing request and diagnose/fix the root cause on their own, without a lengthy explanation first. When an attempted fix doesn't resolve it, the user re-pastes the same console error with a terse nudge ("still error ficx when adding showing fix") and expects the agent to keep debugging until the error is actually gone — a "still error" message means the previous fix was not yet applied/effective, not a new problem description. The user also nudges with a bare "fix" (plus IDE file/line context) when prior explanations haven't led to a fix yet; the attached line selection may point at a symptom location rather than the root cause (e.g. selecting the `../` line in config.php when the actual crash was a missing `global` in a helper), and the agent is expected to trace to the real bug and verify it end-to-end rather than treating the selected line as the diagnosis. Confidence: 0.85
- For product showcase sections, sometimes prefers minimal image-only cards (no price/rating/badge text) with portrait 3:4 images (~300×400, capped at 400px height) and an explicit per-row count (e.g. 4 on desktop) — e.g. simplifying the Best Sellers cards to "keep only image ... height like 300*400". Likewise, for standard card grids the user specifies the exact desktop items-per-row tersely ("in one row show 4 items") and expects the grid column classes updated to match that count. Confidence: 0.7
- Prefers reusing existing data/content for new sections ("data keep same like new arrivals") rather than introducing fresh designs or content — clone the proven template for each new section. Confidence: 0.8
- When creating entirely new pages (About, Contact, Terms, Privacy), expects them to match the home page template exactly — same color palette/theme, header with mega menu (desktop + mobile), and premium footer replicated verbatim ("matching with the template colour like home page") — strict visual consistency across all pages of the site. Confidence: 0.8
- Extends that strict visual consistency to reusable components across pages: when asking to change a component on one page, expects it to match the equivalent component on the home page exactly ("change the category product desiggn like home page products") — same card structure/classes (p-2 padding, rounded-4 square 1:1 image, icon + name + description + lucide star rating/price rows) rather than keeping divergent per-page card styles. This includes matching the rendered size, not just the markup: when the replicated cards came out smaller (3-per-row inside a 9-col area), the user asked to "keep same size also looking big", and the accepted fix switched the grid to 2-per-row on desktop so the cards render as large as the home page's showcase cards. Confidence: 0.85
- Prefers carousel prev/next navigation buttons grouped in the section header beside the "View all" button (one top-right control cluster per section) rather than on a separate row below the carousel — explicitly requested "keep all next prev buttons side of view all button" for every carousel section. Confidence: 0.9
- When requesting a change for one instance, expects it applied to every section/page sharing that element/pattern, not just the single cited occurrence — e.g. "add space in above of this section in all section those have this"; a breadcrumb upgrade requested on about.html was applied to all 4 inner pages sharing the hero class and accepted; a single contact-details update request was applied across all 8 pages (headers, contact page, footer, store locator, policies) and accepted without complaint; reconfirmed when the user asked "in product page add breadcrum like about" so the product page's simple crumb was replaced with the same premium hero; also reconfirmed when a promo-section change (square-on-mobile banners) requested once was pointed at again for the identical second promo section ("show this 2 col square shape in mobile view") — the same `.promo-square` treatment was expected on the second banner too; most recently, after the premium mega menu hover design was approved on the "All Products" panel, the user asked for it on every other nav item ("when hover same day delivery visiting card apprel personalised gift and other also") and all 8 remaining panels were converted to the same design; most recently, after the header/mega-menu redesign on index.html was accepted, the user asked to "apply the header in all pages" and the accepted fix extracted the index.html header block (announcement bar + sticky header + premium mega menu + mobile offcanvas) as a template and replaced the old headers on all 7 other pages (categories, category, product, about, contact, terms, privacy), with index.html serving as the canonical source of truth for shared site chrome. Confidence: 0.98
- Treats the site as a product showcase, not an e-commerce store — so prefers removing all e-commerce features (header "Login / Signup" links, cart icon, "Add to Cart"/"Buy Now" buttons) and replacing them with contact-first CTAs: a prominent "Contact Now" click-to-call button (tel: link) and WhatsApp (wa.me) links/icons in the header, mobile menu, and product page ("no login sign up ... add a contact now button when click will redirect to call"; "in place of cart add a whatsapp icon in header"); the showcase-not-store scope was reconfirmed when planning the database layer — the user chose the minimal catalog-only schema (categories/products/images, no carts, orders, users, or prices) even though full e-commerce was offered as the recommended option. Confidence: 0.97
- For database/back-end work, prefers a minimal, schema-only deliverable — when asked how far the DB plan should go, the user chose the minimal product-showcase schema (products, categories, product_images only, explicitly no e-commerce: "no carts, orders, users, prices, coupons") AND schema only (no PHP/API layer), even though fuller options (full e-commerce, schema + PHP endpoints) were offered as recommended — i.e. deliver the smallest thing that powers the current static pages, not the full path to a store; when the plan is not approved, expects a concise list of adjustment options (more/less tables, different fields, different seed content, include PHP layer) rather than a rewrite. The "minimal" scope is refined by explicit request: while carts/orders/prices/payments remain out of scope, the user asked to also keep a `users` table and a site-wide `settings` table in the approved schema — so user accounts (admin/staff/customer) and a settings store are welcome additions to the catalog-only core. Confidence: 0.8
- For user/account tables, prefers the minimal credentials-only schema — just `username` (unique) and `password_hash` (plus id/created_at), explicitly requested by trimming the users table down to "keep only username and password hashed" and dropping all profile fields (name, email, phone, role, is_active). Passwords must always be stored hashed, never plaintext. Confidence: 0.7
- For database schemas, explicitly wants NO foreign key constraints — "dont use any foregion keys" — preferring plain `INT` reference columns (with indexes for querying) over `FOREIGN KEY ... REFERENCES ... ON DELETE CASCADE` relations; the accepted schema kept `category_id`/`product_id` as indexed columns with no FK constraints, and the user verified 0 FKs afterward. The user re-emphasized the rule again in a follow-up message ("dont use any foregion key") after the work was already done, expecting the agent to verify and confirm zero FKs across the SQL file, the live database, and the plan documentation. Confidence: 0.9
- Prefers a header layout with the logo on the left, a prominent search bar centered in the middle (widened, ~720px max), and contact/action icons on the right — "logo size slight big and searchbar in middle"; reaffirmed when the user asked to remove the utility bar from all pages but explicitly "keep searchbar in middle". Confidence: 0.75
- On mobile, expects the header to collapse to a single row in the explicit order **logo → search → toggle**: logo on the left, the search bar centered in the flexible middle (always visible, ~42px height), and the hamburger toggle at the far right — with the WhatsApp/Contact action buttons hidden on small screens (`d-none d-md-flex`) and no separate second-row search bar. The order was stated explicitly ("the menu for mobile in header show only logo and toggle button and middle search in one row all three correct it") and then pinned down further across the whole site ("keep logo then search thn toggle in one row below navbar menu hide correct in all page"), with the toggle moved to the right end. Confidence: 0.85
- Prefers a clean, minimal header with no auxiliary utility bar above the main header — explicitly asked to remove the Track Order / Store Locator / Sample Kit / Business Solutions strip "from all page", keeping just the logo, the centered searchbar, and the right-side contact/action icons; accepts content cards that merely share a label (e.g. About page's "Business Solutions" feature card) as long as the actual header strip is gone from every page. Confidence: 0.8
- Expects icons in the UI to actually render (reported "whats app icon not visible" when one silently failed); the accepted fix is using real brand glyphs from an icon library already loaded on the page (bootstrap-icons `bi-whatsapp`) rather than generic placeholders from a slower/optional CDN icon framework (lucide `message-circle`) that can silently fail to load — reaffirmed when the user asked "why enquiry now whatsapp not visible" and the same `bi-whatsapp` swap on the Enquiry button was the accepted fix. Confidence: 0.8
- Expects CTAs to be functional rather than decorative placeholders — when the assistant replaced dead links with plain buttons during a redesign, the user pushed back, and the accepted version replaced `href="#"` placeholders with working links (Google Maps directions via `https://maps.google.com/?q=...` and a `tel:` "Call Store" button) while keeping the visual design. Reconfirmed when the user explicitly flagged a "Get Directions" button that didn't redirect ("correct when click rediect to google map in index.html"): the accepted fix replaced a Google Maps *embed* URL (`maps/embed?pb=...`, meant for iframes, which opens the raw embed page when navigated to) with a proper Google Maps search/directions URL (`https://www.google.com/maps/search/?api=1&query=...`) opening in a new tab (`target="_blank" rel="noopener"`) — so every map/directions link on the site must point to a real Google Maps page, not the embed URL. Confidence: 0.7
- Prefers pages/sections to be introduced by a text intro block — an eyebrow badge, a heading, and a supporting paragraph — placed above the main content section (explicitly requested for the contact page: "add a heading with some paragrapgh in above"), delivered as a centered section that flows into the content below. Reaffirmed on the categories page ("add a section heading subheadinng and a paragrapgh"): the accepted block sits between the banner image and the product grid, with a brand-colored eyebrow badge, an h1 whose key phrase is highlighted in brand color, and a max-width paragraph — i.e. the same intro-block pattern and styling is expected reused consistently page to page, not invented fresh each time. Confidence: 0.8
- Prefers the footer "Get in Touch" column to contain only the plain contact rows (address, phone numbers, email, hours) — explicitly removed the "Chat on WhatsApp" button from all footers ("also remove chat on whatsapp"), even though WhatsApp links/icons are kept elsewhere in the site (header/mobile menu). Confidence: 0.7
- Prefers a minimal filter sidebar on category/listing pages — only category checkboxes under a "Categories" heading (with a Clear link), with price sliders and extra filter groups (e.g. "Corners" buttons) removed entirely ("keep only category names in checkbox no corners and price filters"). Confidence: 0.65
- Even in a minimal sidebar, wants ALL categories listed (not just a subset) and presented with a premium look — big/bold category names (larger font size), a lucide icon per category, count badges, brand-colored checkboxes, and hover micro-interactions (tinted background, subtle slide-right) — requested tersely as "show all category with big font size with premium look". Product counts on each category row are explicitly required, not optional ("also show the product counts"), rendered as right-aligned pill badges. Confidence: 0.85
- Avoids duplicating headings between the page hero and the main content area: when the product title (and rating) already appear in the content (e.g. the product card's right column), the page hero should contain only the premium breadcrumb — the earlier hero's duplicated big h1 and rating badge were removed per the user's "show this below breadcrumb" request, leaving the title appearing exactly once, in the content below the breadcrumb. Confidence: 0.6
- Category lists/sidebars must use the site's canonical category set — the same category names and lucide icons as the home page category strip — rather than invented/placeholder filter categories (e.g. made-up finish types with fabricated counts); when the agent's premium sidebar used invented "Matte/Glossy/Velvet/Kraft" checkboxes, the user asked to swap them for the home page's real 18 categories ("looks good but add categories from home page category"), turning the checkboxes into clickable navigation links with a "View All" link. Confidence: 0.75
- Prefers the product detail card kept minimal — only the product name, a category badge, a ~6-line bullet-point description, and the price row; quantity/corner/finish variant selectors and extra info cards (e.g. "What you get") are removed as clutter ("keep only product name category 6lines description"). Consistent with treating the site as a showcase rather than a store. Confidence: 0.7
- Prefers lead/enquiry capture via a modal popup form (Name, Phone, Email, Message) opened by an "Enquiry" button, which on submit routes to WhatsApp with a pre-formatted message to the business number — replacing the direct "Enquire on WhatsApp" wa.me link-button pattern. Confidence: 0.85
- Wants product context (product image link, category, product name) auto-injected into the enquiry message in the background so the customer only fills in name/phone/email/message — the hidden fields are added quietly by the system, not typed by the customer. Confidence: 0.8
-page card style (square rounded image, icon + name + short description) and the standard section header with a "View all" pill — requested as "add a related product section below", upgrading the existing "Pair with" cards to match the home page card template rather than keeping a divergent style. Confidence: 0.6
- Expects interactive UI elements to be reliably clickable — when product-image gallery clicks silently fail (thumbnails not swapping the main image, some gallery photos with no action at all), the user reports tersely ("the image click not work make better") and expects every click path to work: thumbnails swap + highlight the active image, main image/expand opens the zoom modal, and every gallery photo is clickable. Confidence: 0.6
- Prefers the top announcement bar to carry business contact info (business name, location, opening hours, phone number — e.g. "Google Mart Digital Print Shop | Patia, Bhubaneswar | Open Mon–Sat, 9 AM–8 PM | Call +91-7008432909") rather than generic promo/shipping copy, consistent with the site's contact-first positioning; reconfirmed when the reference header the user pasted carried exactly that business-info announcement copy. Confidence: 0.75
- Wants the business's hardcoded site-wide details (logo, announcement text, phone numbers, WhatsApp, email, address, working hours, map link, social URLs, copyright/footer credit) captured in a single editable place in the database (a key→value `settings` table) instead of remaining hardcoded across the 8 pages — explicitly requested "keep a setting for all website details like logo numbers mails address and other s", and the accepted implementation seeded 17 settings rows copied 1:1 from the current hardcoded site. Confidence: 0.8
- Prefers page `<title>` tags to carry the business identity (business name, location, service tagline) rather than leftover template/placeholder titles (e.g. replaced "Classic Visiting Cards — Bootstrap" with "Google Mart – Digital Print Shop in Patia, Bhubaneswar | Fast & Affordable Printing"); explicitly asked for that same title on *all* pages ("change all page meta title Google Mart – ..."), so expects one uniform business-identity title applied site-wide across every page rather than per-page descriptive titles. Confidence: 0.8
- Prefers plans (e.g. database/schema plans) to be delivered as a detailed markdown file saved in the plans directory (e.g. `~/.commandcode/plans/gm-database-plan.md`) rather than as a chat-only summary — explicitly requested "create a md file and details show the plan"; the accepted deliverable was a complete reference document (ERD diagram, full table specs, runnable SQL with seed INSERTs, seed-data mapping, per-page query patterns, design rationale, verification steps, optional next steps) that served as the approved basis for implementation, and the file is expected to be viewable/accessible via the plans UI. Confidence: 0.8
- Prefers the full database plan/documentation to also exist as a markdown file inside the project itself, co-located with the SQL it describes (e.g. `db/DATABASE-PLAN.md` next to `db/gm_db.sql`) — even after the plan was already saved in the agent's plans directory, the user asked "i want all the databse plan in a md file" and the accepted fix copied the complete plan into the project so code and documentation sit in one place. Confidence: 0.6
ry page rather than per-page descriptive titles. Confidence: 0.8
 rather than leftover template/placeholder titles (e.g. replaced "Classic Visiting Cards — Bootstrap" with "Google Mart – Digital Print Shop in Patia, Bhubaneswar | Fast & Affordable Printing"); explicitly asked for that same title on *all* pages ("change all page meta title Google Mart – ..."), so expects one uniform business-identity title applied site-wide across every page rather than per-page descriptive titles. Confidence: 0.8
ry page rather than per-page descriptive titles. Confidence: 0.8
- After approving a plan, expects immediate execution — "execute it now" — and explicitly instructs the agent not to re-enter plan mode or call exit_plan_mode to re-present the approved plan; approval is the green light to implement directly (e.g. approving the DB plan meant extracting the SQL from the plan file, saving it into the project, and importing it into XAMPP MySQL without another planning cycle). Confidence: 0.8
- Treats review notes attached to an approval as non-blocking guidance: expects them honored during implementation (or briefly explained why not) without stalling the approved work for further rounds of approval ("These review notes are NOT blocking; honor them while implementing (or briefly explain why not)"). Confidence: 0.7
- Wants the database plan/documentation to include a detailed "what database for what use" mapping — a section-by-section table showing exactly which table feeds which page/site section (mega menu items, home category rows, sidebar links + counts, breadcrumbs, settings-driven contact/footer blocks, etc.) — requested as "update the md file detailly explain what data base for what use and all"; expects that doc to be kept in sync with the actual schema (after the users table was trimmed to username/password_hash and FKs removed, the plan doc was rewritten to match and all FK references scrubbed). Confidence: 0.8
- Prefers site UI sections to be driven dynamically from the database rather than hardcoded — asked "can i do the mega menu dynamic using this database also home page sections" — i.e. wants the mega menu and home page sections backed by DB queries (categories → menu items/home rows, products → cards via flag columns like is_new/is_trending/is_featured, settings → site-wide text) rather than static HTML. Confidence: 0.8
- When the user hands over exact content for a dynamic feature (e.g. the full mega menu hierarchy with group headings and sub-links), expects it seeded verbatim into the database and rendered exactly as given — DB-driven changes the data source, not the content or the UI design ("the menu keep like as given"). Confidence: 0.7
- Prefers child/related data (e.g. product gallery images) managed inline inside the parent record's add/edit form rather than via a separate admin page — when asked where product gallery images should go, the user replied "i want the product gallery dont keep another keep in product page where related images will add", and the accepted implementation dropped the standalone gallery page (and its nav entry), embedding a "Gallery / Related images" section with multi-file upload, existing-image thumbnails, and per-image delete links directly in the product add/edit form. Confidence: 0.8
- Prefers the project tree kept clean of throwaway debug files — explicitly stopped the agent mid-debug with "dont create dumpfiles.php" when a temporary `$_FILES`-dump script was written into the admin directory; expects debugging to proceed without littering the project (e.g. throwaway test scripts in /tmp, or inspecting existing code instead), and any temp file that does land inside the project should be deleted immediately. Confidence: 0.75
- For admin list pages, prefers pagination over showing all records on one page — explicitly requested "add pegination admin pages"; the accepted implementation paginates categories (12/page) and products (10/page) with prev/next + numbered links and a "Showing X–Y of Z" footer, with a shared `paginate()`/`render_pagination()` helper so every list page behaves the same. Confidence: 0.7
- Prefers admin forms to include image previews — explicitly requested "also image preview add"; the accepted implementation shows a live preview of the selected file before saving (FileReader on file input) alongside the existing image, plus a click-to-zoom modal for thumbnails in tables and galleries. Confidence: 0.7
- Prefers the admin panel to look simple, clean, and user-friendly with properly aligned tables/forms — explicitly asked to "correct the alignment" and make admin pages "looking simple make look good user friendly"; the accepted redesign used a shared sidebar+topbar layout, 2-column aligned form grids, icon-only edit/delete action buttons, status badges, empty states, and stat cards — polished but not decorative, in contrast to the front-end's premium/gradient showcase styling. Confidence: 0.65
- Prefers every site page to be a dynamic `.php` file rather than `.html` — explicitly instructed "dont keep .html do .php" when asking to make the remaining pages dynamic; the accepted deliverable converted all pages to `.php`, set `DirectoryIndex index.php` so the site root serves the dynamic page, rewrote every internal link to point to `.php` (0 remaining), and kept the old `.html` files only as a static fallback. Confidence: 0.85
- For bulk site-wide conversions, explicitly demands speed ("quickly dont take much time") and expects the whole job batched and executed in one fast pass — a single shared include (e.g. `includes/db.php` with DB helpers + settings) plus one conversion script touching all pages at once, followed by end-to-end verification (all pages HTTP 200, lint-clean, real DB data rendering) — rather than slow page-by-page manual work. Confidence: 0.7
- For dynamic navigation, prefers deriving the menu from existing core catalog tables (categories + products) rather than creating dedicated menu tables — explicitly rejected the menu_items/menu_links table approach with "dont create menu table make the mmenu dynamic using category and product table" and expected the seed tables dropped; the accepted pattern maps top-level nav items = categories and panel contents = that category's products, so the menu stays automatically in sync with categories/products added in the admin without extra schema. However, when the user then supplies an exact curated menu hierarchy, that structure overrides the derived menu — the accepted fix embedded the exact 9-item structure as a PHP data array (still no menu table) with sub-links pointing to the dynamic category/product pages. Confidence: 0.75
- Prefers changes made by editing the existing file directly rather than creating a new separate file/module for the component — when the agent extracted the curated 9-item mega menu into a new `includes/menu.php` with a `gm_menu()` function, the user explicitly pushed back with "dont creat menu edit the header.php", and the accepted fix embedded the menu data array inline in `includes/header.php` (and removed the now-unused `$navCats`/`$navProds` queries). New helper/data files for content that belongs in an existing template file are unwelcome; edit in place. Confidence: 0.8
 make look good user friendly"; the accepted redesign used a shared sidebar+topbar layout, 2-column aligned form grids, icon-only edit/delete action buttons, status badges, empty states, and stat cards — polished but not decorative, in contrast to the front-end's premium/gradient showcase styling. Confidence: 0.65
es, and stat cards — polished but not decorative, in contrast to the front-end's premium/gradient showcase styling. Confidence: 0.65
