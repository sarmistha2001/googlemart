- Prefers JavaScript in a dedicated external file (e.g. `assets/js/script.js`) linked from the HTML rather than inline `<script>` blocks — repeatedly and explicitly directs "add js in script.js" / "add all sections scripts in script.js file" for new behavior. Confidence: 0.97
- Prefers well-known slider/carousel libraries (Swiper or Owl Carousel) over hand-rolled custom sliders for carousel sections. Confidence: 0.8
- Prefers carousels/sliders to auto-advance (autoplay) rather than requiring manual swipe/navigation — repeatedly asks for "auto slide" on carousel sections (e.g. "change the best selling section like this with auto slide"). Confidence: 0.9
- Prefers mobile-first responsive carousel density set via the Swiper `slidesPerView` base value plus `breakpoints` in the shared config (e.g. base 1 on mobile → 2 at 576px → 3 at 768px → 4 at 992px+), rather than inline per-slide classes; when the user asks for "one item on mobile" the base `slidesPerView` is the lever to change. Confidence: 0.6
- Accepts small inline `<script>` blocks on a page for page-specific behavior (e.g. the enquiry-modal WhatsApp submission logic) even though site-wide behavior belongs in the external `script.js` file. Confidence: 0.6
- Prefers page-level interactive behavior (e.g. gallery: `setMainImg`, `openZoom`, `pickFromGallery`) implemented as small shared named JS functions with a `data-img` attribute pattern rather than repetitive one-off inline `onclick` statements — a single shared function keeps behavior consistent and fixes silent click failures. Confidence: 0.5
